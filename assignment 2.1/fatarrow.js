//fat arrow funtion of type string takes string and number as input and prints result to console
var fatArrow = function (string, number) { return ("string is " + string + ". the number given is " + number); };
var output = fatArrow("string", 3);
console.log(output);
